version: 2.1
jobs:
  test:
    docker:
      - image: circleci/python:3.6.8
    working_directory: ~/cimr
    resource_class: xlarge
    steps:
      - checkout
      #- run:
      #    name: Install cimr
      #    command: |
      #      bash .circleci/install_cimr.sh
      #- run:
      #    name: Test GWAS HDL Cholesterol (single gz)
      #    command: |
      #      bash .circleci/test_hdl.sh
      #- run:
      #    name: Test GWAS HDL and LDL Cholesterol (tar.gz file)
      #    no_output_timeout: 30m
      #    command: |
      #      bash .circleci/test_gwas_hdl_ldl.sh
      - run:
          name: Test crossmap
          command: |
            bash .circleci/test_crossmap.sh

workflows:
  version: 2
  test:
    jobs:
      - test
