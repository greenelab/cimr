version: 2.1
jobs:
  test:
    docker:
      - image: circleci/python:3.6.8
    working_directory: ~/cimr
    resource_class: xlarge
    steps:
      - checkout
      - run:
          name: Install cimr
          command: bash .circleci/install_cimr.sh
      - run:
          name: dhu big EQTL test
          command: bash .circleci/dhu_eqtl.sh

workflows:
  version: 2
  test:
    jobs:
      - test
